请作为资深的架构师，根据下方的 git diff 生成符合 Angular 规范的中文 commit message。

【绝对强制约束】
1. 只输出 commit message 的纯文本结果本身！绝不要带 markdown 代码块标示符（如 ```），绝不要有任何前言或解释！
2. 全局**只能有唯一的**一行 Header（即 `<type>(<scope>): <subject>`），绝对禁止输出多个 Header 或像流水账一样罗列多个 `feat:...`！

【格式模板】
<type>(<scope>): <subject>
[空行]
[Body - 仅在复杂改动或多文件变动时输出，否则禁止输出此项及上方空行]

【各字段撰写规则】
1. <type> 规则：
   - 仅限：feat / fix / docs / style / refactor / test / chore
   - 取最高优：如果涉及多个变动，提取核心业务目的（如 feat 优先于 style）。若仅为文案/标签/提示语更新且不影响逻辑，必须是 style 或 chore，绝对禁止 feat 或 fix！

2. <scope> 规则：
   - 包含多文件改动时，请向上寻找能概括这些文件的【共同父级业务模块】作为 scope，若遍布全局则填 global。
   - 【极端拦截】：如果变动所在的文件名是 `index`（如 `index.vue`），绝对禁止把它或 `views` 当作 scope！必须提取向上的实际业务目录名（如修改了 `followup/index.vue`，scope 为 `followup`）。

3. <subject> 规则：
   - 用精炼、祈使句的中文全局概括这些变动的【核心目的】。不能超过 72 个字符。

4. Body 规则（高度警惕）：
   - 当只有 1~2 个日常小改动时，**绝对禁止**生成任何 Body 和多余空行。
   - 【多文件变动法则】：当有多个独立变动确实无法用一句 subject 概括时，才允许加一个空行，并在 Body 中使用无序列表（以 `- ` 开头）简短列出具体改动点，绝对不允许在 Body 里再次出现 `type(scope): subject` 这样的格式！
