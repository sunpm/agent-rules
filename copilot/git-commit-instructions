请作为资深的架构师，根据下方的 git diff 生成符合 Angular 规范的中文 commit message。

【绝对强制约束】
只输出 commit message 的纯文本结果本身！绝不要带有 markdown 代码块标示符（如 ```），绝不要包含任何解释、前言、问候或分析过程！

【格式模板】
<type>(<scope>): <subject>
[空行]
[Body - 仅限极复杂重构时输出，否则禁止输出此项及上方空行]

【各字段撰写规则】
1. <type> 规则：
   - 仅限：feat / fix / docs / style / refactor / test / chore
   - 特别注意：如果仅仅是文案、标签、提示语的更新，且不影响核心逻辑，类型【必须】是 style 或 chore，绝对禁止使用 feat 或 fix！

2. <scope> 规则：
   - 指明改动影响的业务模块或组件。
   - 拦截规则：如果变动所在的文件名是 `index`（例如 `index.vue`, `index.ts`），**绝对禁止**使用 `index` 或 `views` 作为 scope！你必须向上的文件路径中提取真正具备业务意义的目录名（例如：修改了 `followup/index.vue`，scope 应为 `followup`）。

3. <subject> 规则：
   - 用精炼、祈使句语气的中文概括改动。
   - 长度不得超过 72 个字符。

4. Body 规则（高度警惕）：
   - 对于日常中小型变动（如页面调整、普通 Bug 修复、文案修改），**绝对禁止生成任何 Body，也不要输出多余的空行**。只有 Header 这一行。
   - 仅仅在涉及极其复杂的底层核心重构、打破向下兼容或需要强说明“为什么要这么做（Why）”时，才允许加一个空行并简短书写 Body。
